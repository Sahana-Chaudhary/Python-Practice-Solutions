# replace double space with single space

a= "You are  a  great person"
print(a.replace("  "," "))     #strings are immutable means u cannot change them by running functions on them
print(a.replace(" a ","the"))
